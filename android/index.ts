// English. 中文.
// Expo entry point that registers the App component as the React Native root.
// Expo 入口, 将 App 组件注册为 React Native 根组件.

import { registerRootComponent } from "expo";

import App from "./App";

registerRootComponent(App);
